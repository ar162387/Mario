package minigames.client.deepfried.entities;

public class SendOrderWindow extends Entity {
    /**
     * The send order window class that defines the send order window where completed orders are sent
     */

    
    // Constructor
    public SendOrderWindow(int x, int y) {
        super(x, y);
        loadImage("/deepFried/sendOrderWindow.png");
    }
    
     
}

//import java.awt.*;
//import java.awt.event.*;
//import javax.swing.*;

/*GameMoves class to handle game logic and rendering*/
/* class GameMoves extends JPanel implements KeyListener, ActionListener, MouseListener {
    private static final long serialVersionUID = 1L;

    /* Layout object to draw the board*/
    //Layout la; 

    /* Build_Player object to initialize and manage players*/
    //Build_Player p; 
    /*Timer object for handling animations or timed events*/
    //Timer time; 

 /*    public GameMoves() { */
        // Initialize the layout and players
/*         la = new Layout(50, 50); 
        p = new Build_Player(30, 30);  */

        /*add key listener and mouse listner events*/
/*         addKeyListener(this);
        addMouseListener(this); 
        setFocusable(true);
        time = new Timer(100, this); 
        time.start(); 
    } */

    //public void paintComponent(Graphics g) {
        //super.paintComponent(g); 
        /*Draw the board layout*/
        //la.draw((Graphics2D) g); // 

        /*// Draw the players and pawns*/
        //p.draw((Graphics2D) g); 
    //}

    //public void actionPerformed(ActionEvent e) {
        /*Repaint the board and pawns when the timer triggers*/
        //repaint(); 
    //}

    // Key events handling
    //public void keyPressed(KeyEvent e) {
        /*Add code to handle key presses if needed*/
    //}

    ///public void keyReleased(KeyEvent e) {

    //}

    //public void keyTyped(KeyEvent e) {

    //}

    /*Mouse events handling*/
    //public void mousePressed(MouseEvent e) {
        // Add code to handle mouse clicks if needed
/*     }

    public void mouseReleased(MouseEvent e) {}
    public void mouseEntered(MouseEvent e) {}
    public void mouseExited(MouseEvent e) {}
} */

// Main class to start the game
/* public class Main {
    public static void main(String[] args) {
        JFrame frame = new JFrame("Ludo Game");
        GameMoves game = new GameMoves();
        frame.add(game);
        frame.setSize(800, 800);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setVisible(true);
    }
}
 */ //*/